#!/bin/bash
#-------------------------------------------------------------------------------------------
# Instalacion del plugin /opt/cnm-sp/snmp-linuxOS
#-------------------------------------------------------------------------------------------

[ -f /opt/cnm-sp/snmp-linuxOS/pre-install ] && { /opt/cnm-sp/snmp-linuxOS/pre-install || exit $?; }

/update/db/db-manage.php -p /opt/cnm-sp/snmp-linuxOS || exit $?

[ -f /opt/cnm-sp/snmp-linuxOS/post-install ] && exec /opt/cnm-sp/snmp-linuxOS/post-install
exit 0
